# Railway Deployment Configuration - Streamlit
[build]
  builder = "nixpacks"

[deploy]
  startCommand = "streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"

[build.env]
  NIXPACKS_PYTHON_VERSION = "3.10"